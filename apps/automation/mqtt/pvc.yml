# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: mqtt-pvc
#   namespace: automation  
# spec:
#   accessModes:
#     - ReadWriteOnce
#   resources:
#     requests:
#       storage: 1Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: mqtt
  namespace: automation 
  name: mqtt-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 10Gi
  volumeName: pv-nfs-mqtt-config
---
