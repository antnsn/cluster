apiVersion: v1
kind: Service
metadata:
  labels:
    app: torrent
  namespace: torrent 
  name: svc-rutorrent
spec:
  ports:
    - name: "http"
      port: 8112
      targetPort: http-torrent 
    - name: "torrent-tcp"
      port: 6881 
      protocol: TCP
      targetPort: torrent-tcp    
    - name: "torrent-udp"
      port: 6881 
      protocol: UDP
      targetPort: torrent-udp

  selector:
    app: torrent   
